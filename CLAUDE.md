# Claude Code Instructions

Read [`AGENTS.md`](AGENTS.md) first. It is the canonical repository-wide contract.

Then read the nearest nested `AGENTS.md` for rules local to the files being changed. Load a profile from [`.github/agents/`](.github/agents/README.md) only when the task matches that specialist scope. Human-facing references live under `docs/`; they are not additional automatic contracts.

Before finishing, run the smallest applicable validation gate and report the real result.
