(() => { const f=document.getElementById('privacyRequestForm'); if(!f)return; const st=document.getElementById('privacyRequestStatus'); f.onsubmit=async e=>{e.preventDefault();const b=f.querySelector('button');b.disabled=true;st.textContent='Enviando…';try{const t=await (await fetch('/api/csrf-token',{credentials:'same-origin'})).json();const r=await fetch('/api/privacy/requests',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':t.csrfToken},body:JSON.stringify({request_type:privacyType.value,email:privacyEmail.value,message:privacyMessage.value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||'Não foi possível enviar a solicitação.');st.textContent='Solicitação recebida. O retorno será feito pelo canal informado.';f.reset()}catch(e){st.textContent=e.message||'Não foi possível enviar a solicitação.';st.classList.add('error-message')}finally{b.disabled=false}}})()